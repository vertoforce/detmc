# Copy to .env before running any harness script:
#   cp .env.example .env && sed -i "s/changeme/$(openssl rand -hex 16)/" .env
# The value only has to be the same for both halves of a pair, so the generated
# server.properties match. .env itself is gitignored.
RCON_PASSWORD=changeme

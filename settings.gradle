pluginManagement {
    repositories {
        maven { name = 'Fabric'; url = 'https://maven.fabricmc.net/' }
        maven { name = 'NeoForged'; url = 'https://maven.neoforged.net/releases' }
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = 'detmc'
include 'common'
include 'fabric'
// Phase 1: settings-included stub only. Build is not wired up yet (see README).
include 'neoforge'
